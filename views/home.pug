extends layouts/main.pug
include mixins/videoBlock

block content  
    .videos
        each item in videoList
            +videoBlock({
                videoFile: item.videoFile,
                title: item.title,
                views: item.views

            })